# Technical Architecture

The technical architecture has not been selected yet.

This document should be updated when decisions are made about areas such as:

- client applications;
- backend/API structure;
- authentication;
- database and persistence;
- real-time Game synchronization;
- hosting and deployment.

Important choices should be recorded separately in [`decisions/`](./decisions/) so the reasoning remains visible over time.
